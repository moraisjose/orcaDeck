"""orcad's version — kept on its own so both orcad.py (the entry point,
prints it at startup) and server.py (the HTTP `Server` header) can import it
without either importing the other."""

VERSION = "0.2.1"
